# Print bridge dependencies.
# brother_ql_next is a maintained fork of brother_ql that works with modern
# Pillow and Python 3.11+ (the original pins to Pillow <10). It imports as
# `brother_ql`, so no code changes are needed.
requests>=2.31,<3
Pillow>=11
# NOT the original `brother-ql`. Both packages install a module called
# brother_ql, so having both in one environment means whichever was written
# last wins — and the original lacks the DK-1234 die-cut label (60x86), which
# makes badge.py fall back to 62 mm continuous. Badges then look almost right,
# because the printer positions to the physical label whatever it is sent, so
# the only symptom is a geometry nobody chose. A dev machine had both for
# weeks and its badge tests measured the fallback.
#
# If `pip list` shows brother-ql alongside this, uninstall it.
brother-ql-next>=0.12
python-dotenv>=1.0,<2
